#ifndef COMMON_H
#define COMMON_H

#include "ms_data.h"
#include "pwc_block.h"


extern SlaveConfig g_slaveCfg;

#define MAX(a, b) ((a) > (b) ? (a) : (b))
#define MIN(a, b) ((a) < (b) ? (a) : (b))

#endif // COMMON_H
